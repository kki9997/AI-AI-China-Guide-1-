## Packages
leaflet | Core mapping library
react-leaflet | React components for Leaflet maps
framer-motion | For smooth page transitions and UI animations
lucide-react | Icon library (already in base, but listing for completeness)
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Zcool XiaoWei'", "'Cinzel'", "serif"],
  body: ["'Lato'", "'Noto Sans SC'", "sans-serif"],
}

Additional CSS:
Need to import Leaflet CSS in index.css or index.html
